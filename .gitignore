# System Files
.DS_Store
Thumbs.db
Desktop.ini

# Dependencies
node_modules/

# Locks (keep pnpm-lock.yaml — it is the project's lockfile)
yarn.lock

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Mastra AI Framework caches & DBs
.mastra/
*.db
*.db-journal
*.db-shm
*.db-wal
output.txt

# Build outputs and framework caches
.next/
out/
build/
dist/
.turbo/

# Supabase local development caches
supabase/.branches/
supabase/.temp/
.supabase/

# Claude Code local files (entire directory — settings, memory, transcripts)
.claude/

# MCP configuration (may contain server URLs and tokens)
.mcp.json

# Environments, tokens, and secrets
# .env.example is intentionally committed — it contains key names only, no values.
# All other .env files are ignored.
.env*.local
.env.production
.env.staging
.env.development
.secrets

# TypeScript build info
tsconfig.tsbuildinfo

.agents/
